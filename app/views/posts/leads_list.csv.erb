<%- headers = ['Nome', 'Email', 'Tipo', 'IP', 'Data de Cruacao'] -%>
<%= CSV.generate_line headers %>
<%- @users.each do |user| -%>
<%= CSV.generate_line([user.name, user.email, (user.isPJ ? 'B2B' : 'B2C'), user.ip, user.created_at]) -%>
<%- end -%>